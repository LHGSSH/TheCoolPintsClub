const mongoose = require('mongoose');
//const Inventory = mongoose.model('');

module.exports = {
    /**
     * 
     * @param {*} req 
     * @param {*} res 
     */
    checkout: async function (req,res){

    }
}